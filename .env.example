# Copy to .env and fill in real values. Never commit .env.

PORT=3000
NODE_ENV=production

# SQLite file path (used if DB_DRIVER=sqlite)
DB_DRIVER=sqlite
SQLITE_PATH=./data/bills.db

# JWT
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=7d

# Scraper
SCRAPE_DELAY_MS=1500
